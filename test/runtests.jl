using HydrogenDimerMRCC
using Test

# add tests making sure that errors are thrown when R, r1, r2, are not positive
# add tests making sure that errors are thrown when theta1, phi1, etc. are outside their
# proper ranges

# add tests checking for certain easy-to-verify cases (sin(theta1) is sqrt(2.0), etc.)
